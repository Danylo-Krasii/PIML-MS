"""Physics-encoded surrogates for stress localization in 3D polycrystals.

The modules are run as scripts, for example `python -m piml.infer --help`.
Paths are configured in `piml.paths`.
"""

__version__ = "0.1.0"
